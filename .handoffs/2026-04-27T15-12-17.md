# Handoff — 2026-04-27 (session 4)

## Pick up next

1. **Port slash commands from claude-presence** — biggest UX win, no code changes to sangha. Create `commands/` dir with `/register`, `/presence`, `/claim`, `/release`, `/broadcast`, `/inbox`. See `docs/port-from-claude-presence.md` for details and `~/soft/claude-presence/commands/` for reference implementations.

2. **Port UserPromptSubmit hook** — passive session awareness. On every prompt, inject a one-liner when other sessions or locks exist. Reference: `~/soft/claude-presence/hooks/user-prompt-submit.sh`.

3. **Verify stdio reliability** — the config is switched (`~/.claude/settings.json` now uses `sangha serve --stdio`). Next few sessions should confirm whether stale-session and dropped-connection problems are gone.

4. **v0.2 review findings** — 15 items deferred from `docs/reviews/opus47-initial-review.md`. Key: unify timestamp types (#15/#16), add pagination (#17), expand tool descriptions (#19), loopback guard (#21), move validate.rs (#27).

## Context for next session

- Config changed: sangha MCP is now stdio, not HTTP daemon. Takes effect on new CC sessions.
- Two new docs: `docs/port-from-claude-presence.md` (what to port), `docs/daemon-sketch.md` (parked daemon idea)
- HEAD is unchanged from prior session (no code changes this session, only docs + config)
- The daemon is still installed and runnable (`sangha serve` for HTTP, `sangha serve --stdio` for stdio) — nothing was removed

## Blockers

None.
