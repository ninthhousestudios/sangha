# Handoff

## Status

sangha v0.1.0 implementation complete. 84 tests passing, clippy clean.
Two code reviews done (GLM-5.1, Kimi-k2.6) — fixes triaged but not yet applied.

## What to pick up next

### 1. Review fixes (8 fixes + 3 comments)

Reviews are at `docs/reviews/glm51-v010-review.md` and `docs/reviews/kimik26-v010-review.md`.

**Fixes to implement:**

| ID | What | Where |
|----|------|-------|
| C1 | Extract main() subcommands into per-command functions (CC=28 → ~5 each) | `src/main.rs` |
| C2 | Replace `unwrap()` with `if let Some` in Identity::bind Err branch | `src/identity.rs:33` |
| H4 | **Bug**: Identity::bind ignores project mismatch on re-bind with same session_id — add project equality check in idempotent path | `src/identity.rs:31-34` |
| H5 | Remove `session_recreated` dead code (always false, never appears in output) | `src/db.rs:126`, `src/tools/locks.rs:79` |
| H6 | Remove unused `gethostname` crate from Cargo.toml | `Cargo.toml:35` |
| M1 | Extract `format_ms()` to shared module (duplicated in presence.rs, locks.rs, inbox.rs) | `src/tools/*.rs` |
| M6 | Cap `read_inbox` default limit (currently `i64::MAX`) — default 100, hard cap 1000 | `src/db.rs:632` or handler layer |
| M7 | Populate lock `branch` from session row (currently hardcoded `None`) | `src/tools/locks.rs:130` |

**Comments to add:**

| ID | What | Where |
|----|------|-------|
| H1 | Comment explaining heartbeat TTL is correct (reset to full because last_heartbeat just updated) | `src/db.rs:326` |
| H3 | Document benign mutex gap between prune and query | `src/db.rs:357` |
| M3 | Note what replaces deprecated `any_service` | `src/main.rs` (the `#[allow(deprecated)]` line) |

**Skipped (with rationale):**
- C3 (HolderInfo duplication): standard Rust layering, DB i64 vs API String
- H2 (background pruning): lazy pruning fine for v1 scale
- M2/M4/M5/M8: low impact, benign behaviors

### 2. Live test

Start the daemon and exercise it from a real Claude Code session:

```bash
cargo build --release
sangha serve
```

Then in Claude Code MCP settings, add:
```json
{
  "sangha": {
    "type": "streamable-http",
    "url": "http://127.0.0.1:3200/mcp"
  }
}
```

Test: register a session, claim a lock, broadcast a message, verify with `sangha status --json` and `sangha locks --json`.

### 3. Setup for daily use

- Add CLAUDE.md fragment to projects that should use sangha (template in `docs/future-hooks.md`)
- Create systemd user unit (or launchd plist) for auto-start on login
- Update /done and /reflect skills to claim advisory locks before writing artifacts

## Key context

- Plan: `.agents/plans/2026-04-26-sangha-implementation.md`
- Reviews: `docs/reviews/glm51-v010-review.md`, `docs/reviews/kimik26-v010-review.md`
- `handle_claim` takes `config: &Config` as extra param (Db.config is private)
- SanghaServer has manual Clone impl — fresh Identity per clone (correct semantics)
- Default CLI command is Status (not Serve) — per plan spec
