# Handoff — 2026-04-27 (session 2)

###

it takes time for the sessions to unregister. it sort of annoys me, because then i cant
be sure if its really accurate or not.
i didnt note the time, but the session i exited from was still listed minutes later.

idea: check how ~/soft/claude-presence/ deals with this. maybe we should build and run
it and see how it works
and then a session that i was currently actually using dropped but the session i had
exited was still there

claude was working for 17+ minutes. session dropped out.

## What's done

Session lifecycle is solid now. Four commits landed:
- MCP tools discovery fix (schemars boolean schema)
- WIP batch commit (refactors, hardening, dead code removal)
- Auto-unregister via Drop on MCP disconnect
- Keep-alive 900s / TTL 1800s tuning

Binary at `~/.local/bin/sangha` matches HEAD (`2bc44d5`). Service is running.

## Pick up next

1. **Gemini session_register visibility** — Gemini connects to sangha but its model doesn't follow the hook-injected instruction to call session_register. Not a correctness bug (Drop cleans up), but sessions don't appear in `sangha status`. Options: stronger hook wording, Gemini-specific GEMINI.md instruction, or accept the gap.

2. **`sangha locks` text output** — doesn't print branch (JSON mode does). One-liner fix in `cmd_locks` if it bothers you.

3. **Consider a `sangha register` CLI command** — would let shell hooks register sessions directly without going through MCP. Useful for clients whose models don't cooperate (Gemini). Not urgent.

## Context for next session

- Hook timeout was 3ms (not 3s) — fixed in both `~/.claude/settings.json` and `~/.gemini/settings.json` to 3000ms.
- `~/.claude.json` is the authoritative MCP config for CC discovery. `~/.claude/settings.json` mcpServers is redundant but don't remove it — Josh has been burned by that before.
- Session cleanup layering: Drop (immediate) → keep-alive ping (900s) → TTL prune (1800s backstop).

## Blockers

None.
